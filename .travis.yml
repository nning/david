language: ruby

os:
  - linux
  - osx

rvm:
  - 2.1
  - jruby

matrix:
  allow_failures:
    - os: osx
    - rvm: jruby

notifications:
  email: false

script:
  - bundle exec rake build
  - bundle exec rake
