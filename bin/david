#!/usr/bin/env ruby

require_relative '../lib/david'
require_relative '../lib/rack/hello_world'

debug = 'none'
debug = 'debug' if ARGV.include?('-d')

klass = Rack::HelloWorld.new
klass = Rack::Lint.new(klass) if ARGV.include?('-l')

Rack::Handler::David.run(klass, :Log => debug)
